# Study Todo (Vite + React)

A minimal todo app for lecture-aligned study planning (9:00–18:00 lecture day).

Setup:

1. cd study-todo
2. npm install
3. npm run dev

The app stores todos in localStorage. Add a time to flag items that fall inside lecture hours (9–18).
